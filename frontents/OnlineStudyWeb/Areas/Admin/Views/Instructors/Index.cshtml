@model System.Collections.Generic.List<Business.Dtos.Contact.InstructorDto>

@{
    ViewBag.Title = "title";
    Layout = "_AdminLayout";
    int i = 0;
}
          <div class="content-wrapper">
            <div class="row" id="proBanner">
            </div>
            <div class="page-header">
              <h3 class="page-title">
                <span class="page-title-icon bg-gradient-primary text-white mr-2">
                  <i class="mdi mdi-school"></i>
                </span> Instruktører
              </h3>
            </div>
            <div class="row">
              <div class="col-12 grid-margin">
                <div class="card">
                  <div class="card-body">
                    <div class="table-responsive">
                       @if (Model.Any())
                              {
                                  <table class="table">
                                      <tr>
                                          <tr>
                                              <th>#</th>
                                              <th>Bilde</th>
                                              <th>Navn</th>
                                              <th>Email</th>
                                              <th>TelefonNummer</th>
                                              <th>Tittel</th>
                                              <th>Beskrivelse</th>
                                              <th>Opprettet dato</th>
                                          <th>Handling</th>
                                          <th>Handlig</th>
                                      </tr>
                                      @foreach (var item in Model)
                                      {
                                          
                                          <tr>
                                              @{i = i + 1;}
                                              <td>@i</td>
                                              <td><img src="@item.ImageUrl" class="rounded-circle" style="max-width: 50px; max-height: 50px;" /></td>
                                              <td><i class="fab fa-angular fa-lg text-danger me-3"></i> <strong>@item.FullName</strong></td>
                                              <td>@item.Email</td>
                                              <td>@item.PhoneNumber</td>
                                              <td>@item.Title</td>
                                              <td><span class="badge bg-label-primary me-1">@item.Description</span></td>
                                              <td> @(((DateTime)item.CreatedDate).ToString("dd-MMM-yyyy"))</td>
                                              <td>
                                                  <a asp-action="Update" asp-area="Admin" asp-controller="Instructors" asp-route-id="@item.Id" class="btn btn-warning"><i class="fa fa-edit"></i> Rediger</a></td>
                                              <td>
                                                  <a asp-action="Delete" asp-area="Admin" asp-controller="Instructors" asp-route-id="@item.Id" class="btn btn-danger" onclick="return confirm('Are you sure you want to delete this item?')"><i class="fa fa-trash"></i> Slett</a>
                                              </td>
                                              
                                          </tr>
                                      }
                                  </table>
                                  <br/>
                                 
                              }  
                              else
                              {
                                  <div class="alert alert-info">
                                        <strong>Info!</strong> Ingen Instruktør funnet.
                                  </div>
                              }
                        <a asp-action="Create" asp-area="Admin" asp-controller="Instructors" class="btn btn-primary"><i class="mdi mdi-plus"></i> Legg til nytt kurs</a>
                    </div>
                  </div>
                </div>
              </div>
            </div>
          </div>
          <!-- content-wrapper ends -->
          <!-- partial:partials/_footer.html -->
       
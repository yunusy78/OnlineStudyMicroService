@model List< Business.Models.DiscountViewModel.DiscountUpdateViewModel>
@{
    ViewBag.Title = "title";
    Layout = "_AdminLayout";
    int i = 0;
}
          <div class="content-wrapper">
            <div class="row" id="proBanner">
            </div>
            <div class="page-header">
              <h3 class="page-title">
                <span class="page-title-icon bg-gradient-primary text-white mr-2">
                  <i class="mdi mdi-shopping"></i>
                </span> Rabatt Koder
              </h3>
            </div>
            <div class="row">
              <div class="col-12 grid-margin">
                <div class="card">
                  <div class="card-body">
                    <div class="table-responsive">
                       @if (Model.Any())
                              {
                                  <table class="table">
                                      <tr>
                                          <tr>
                                              <th>#</th>
                                              <th>Kode</th>
                                              <th>Prosent</th>
                                              <th>Status</th>
                                              <th>BrukerId</th>
                                              <th>Opprettet dato</th>
                                          <th>Handling</th>
                                          <th>Handlig</th>
                                      </tr>
                                      @foreach (var item in Model)
                                      {
                                          
                                          <tr>
                                              @{i = i + 1;}
                                              <td>@i</td>
                                              <td><i class="fab fa-angular fa-lg text-danger me-3"></i> <strong>@item.Code</strong></td>
                                              <td>@item.Rate</td>
                                              <td>@item.Status</td>
                                              <td><span class="badge bg-label-primary me-1">@item.UserId</span></td>
                                              <td> @(((DateTime)item.CreatedTime).ToString("dd-MMM-yyyy"))</td>
                                              <td>
                                                  <a asp-action="Update" asp-area="Admin" asp-controller="Discount" asp-route-id="@item.Id" class="btn btn-warning"><i class="fa fa-edit"></i> Rediger</a></td>
                                              <td>
                                                  <a asp-action="Delete" asp-area="Admin" asp-controller="Discount" asp-route-id="@item.Id" class="btn btn-danger" onclick="return confirm('Are you sure you want to delete this item?')"><i class="fa fa-trash"></i> Slett</a>
                                              </td>
                                              
                                          </tr>
                                      }
                                  </table>
                                  <br/>
                                 
                              }  
                              else
                              {
                                  <div class="alert alert-info">
                                        <strong>Info!</strong> Ingen rabatt funnet.
                                  </div>
                              }
                        <a asp-action="Create" asp-area="Admin" asp-controller="Discount" class="btn btn-primary"><i class="mdi mdi-plus"></i> Legg til nytt discount</a>
                    </div>
                  </div>
                </div>
              </div>
            </div>
          </div>
          <!-- content-wrapper ends -->
          <!-- partial:partials/_footer.html -->
       